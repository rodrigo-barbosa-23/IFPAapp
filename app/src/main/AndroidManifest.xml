<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ifpaapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditarBensCategoria"></activity>
        <activity android:name=".CategoriasBens" />
        <activity android:name=".GerarRelatorioPorCategoria" />
        <activity android:name=".ConsultarBensPorCategoria" />
        <activity android:name=".GerarRelatorioPorSetor" />
        <activity android:name=".ConsultarBensPorSetor" />
        <activity android:name=".CadastrarBens" />
        <activity android:name=".EditarBensSetor" />
        <activity android:name=".PatrimonioMovel" />
        <activity android:name=".MenuApp" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>